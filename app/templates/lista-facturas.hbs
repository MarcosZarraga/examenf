{{outlet}}
<h3>Lista Facturas</h3>


<div class="row">
        <div class="col s12 m6">
          <div class="card blue-grey darken-1">
            <div class="card-content white-text">
              <span class="card-title">{{model.folio}}</span>
              <p>Fecha:</p>{{model.fecha}}
              <p>Serie:</p>{{model.serie}}
            </div>
            <div class="card-action">
				{{model.subTotal}}
            </div>
          </div>
        </div>
      </div>